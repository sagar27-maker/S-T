#include <stdio.h>
#include <stdlib.h>
int main()
{
    int month[12]={31,28,31,30,31,30,31,31,30,31,30,31};
    int d,m,y,nd,nm,ny,ndays;
    printf("Enter day month year: ");
    scanf("%d%d%d",&d,&m,&y);
    if(y < 1812 || y > 2012)
    {
        printf("Invalid Input Year");
        return 0;
    }
    if(m < 1 || m > 12)
    {
        printf("Invalid Input Month");
        return 0;
    }
    ndays = month[m-1];
    if(m == 2)
    {
        if((y%400==0) || (y%4==0 && y%100!=0))
            ndays = 29;
    }

    if(d < 1 || d > ndays)
    {
        printf("Invalid Input Day");
        return 0;
    }
    nd = d + 1;
    nm = m;
    ny = y;
    if(nd > ndays)
    {
        nd = 1;
        nm++;
    }
    if(nm > 12)
    {
        nm = 1;
        ny++;
    }
    printf("\nGiven date: %d:%d:%d",d,m,y);
    printf("\nNext date: %d:%d:%d",nd,nm,ny);
    return 0;
}
